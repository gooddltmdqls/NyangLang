const isInt = require('./int.js');
const isBoolean = require('./boolean.js');
const { isString, parseString } = require('./string.js');

module.exports = {
    isInt,
    isBoolean,
    isString,
    parseString
}